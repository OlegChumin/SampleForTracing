# SampleForTracing

Initial workspace for sample microservices related to tracing experiments.

## Structure

- `services/tracing-service` - starter Spring Boot microservice with a basic health-oriented API.

## Run

```powershell
./gradlew.bat :tracing-service:bootRun
```

## Test

```powershell
./gradlew.bat test
```
